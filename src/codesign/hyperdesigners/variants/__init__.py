"""One module per training algorithm.

Each holds that algorithm's own networks, loss, training loop and config wiring. They are
re-exported from ``codesign.hyperdesigners``; import them from there.
"""
